// Наши редьюсер для users

const initialUsersState = {}; // Начальное состояние

// сам редьюсер
export const usersReducer = (state = initialUsersState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
