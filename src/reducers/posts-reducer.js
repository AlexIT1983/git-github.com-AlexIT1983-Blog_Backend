// Наши редьюсер для posts

const initialPostsState = {}; // Начальное состояние

// сам редьюсер
export const postsReducer = (state = initialPostsState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
